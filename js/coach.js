import {save,S,today} from './storage.js';
import {recoveryScore,coachRules} from './recovery.js';
import {chooseMission} from './missions.js';
export function renderCoach(ctx){const {profile,coach,log,missions,onSave,onGo}=ctx;const score=recoveryScore(coach);const m=chooseMission({missions,profile,coach,log});const intensity=score<45?'Tática':score<70?'Normal':'Pesada';document.querySelector('#coach').innerHTML=`<div class="card"><div class="label">Abertura · Coach</div><div class="h2">Como estás hoje?</div><div class="small">Responde em 20 segundos. A missão adapta-se ao sono, energia, dor muscular, outro treino e uso da Power Twister.</div></div><div class="card"><div class="row"><div><div class="label">Recovery Score</div><div class="big ${score<45?'bad':score<70?'warn':'ok'}">${score}%</div></div><div class="pill">${intensity}</div></div><div class="bar"><div class="fill" style="width:${score}%"></div></div><div class="small">${score<45?'Hoje: proteger recuperação.':score<70?'Hoje: progressão técnica, sem falhar.':'Hoje: boa janela para carga e excêntricas lentas.'}</div></div><div class="card"><div class="grid2"><div class="field"><label>Horas de sono</label><input id="sleep" type="number" min="3" max="10" step="0.5" value="${coach.sleep}"></div><div class="field"><label>Energia 1–5</label><input id="energy" type="number" min="1" max="5" value="${coach.energy}"></div><div class="field"><label>Dor muscular 0–5</label><input id="soreness" type="number" min="0" max="5" value="${coach.soreness}"></div><div class="field"><label>Outro exercício</label><select id="sport"><option value="none">Nenhum</option><option value="karate">Karaté</option><option value="futebol">Futebol</option><option value="trail">Trail</option><option value="natacao">Natação</option></select></div></div><div class="grid2"><div class="field"><label>Power Twister 40 kg?</label><select id="bar"><option value="true">Sim</option><option value="false">Não</option></select></div><div class="field"><label>Foco desejado</label><select id="plan"><option value="auto">Automático</option><option value="A">Peito + tríceps</option><option value="B">Ombros</option><option value="C">Costas + bíceps</option><option value="D">Core</option><option value="E">Braços</option><option value="R">Recuperação</option></select></div></div><div class="field"><label>Notas livres</label><textarea id="notes" rows="3">${coach.notes||''}</textarea></div><div class="grid2"><button class="btn primary" id="generateMission">Gerar missão</button><button class="btn soft" id="saveCoach">Guardar estado</button></div></div><div class="card"><div class="label">Missão proposta</div><div class="h2">${m.title}</div><div class="small">${m.focus} · ${m.phase}</div>${coachRules(coach,score).map(x=>`<div class="item"><div><b>${x[0]}</b><span class="small">${x[1]}</span></div></div>`).join('')}</div>`;document.querySelector('#sport').value=coach.sport;document.querySelector('#bar').value=String(coach.bar!==false);document.querySelector('#plan').value=profile.plan||'auto';function collect(){const c={sleep:+sleep.value,energy:+energy.value,soreness:+soreness.value,sport:sport.value,bar:bar.value==='true',notes:notes.value,checkedAt:today()};profile.plan=plan.value;save(S.coach,c);save(S.profile,profile);return c}saveCoach.onclick=()=>onSave(collect());generateMission.onclick=()=>onGo(collect())}
